@model List<Product>
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResource> Localizer

@* @if(TempData["success"] != null)
{
    <h2>@TempData["success"]</h2>
} *@
@* call partial view to display the result of temp  *@
@* <partial name="_Notification" /> *@

<div class="container">
    <div class="row pt-4 pb-3">
        <div class="col-6">
            <h2 class="text-primary">
               @Localizer["ProductJerseyListMsg"]
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-info">
                <i class="bi bi-plus-circle"></i>@Localizer["AddNewProductMsg"]
            </a>
        </div>
    </div>

    <br />
    <br />
    <table id="tblData" class="table table-bordered table-striped" style="width:100%">
        <thead class="table-primary">
            <tr>
                <th class="fw-bold">@Localizer["ProductMsg"]</th>
                <th class="fw-bold">@Localizer["SeasonMsg"]</th>
                <th class="fw-bold">@Localizer["CategoryMsg"]</th>
                <th class="fw-bold">@Localizer["SleeveTypeMsg"]</th>
                @* <th class="fw-bold">Edition</th> *@
                <th class="fw-bold">@Localizer["SizeMsg"]</th>
                <th class="fw-bold">@Localizer["PriceMsg"]</th>
                @* <th class="fw-bold">Remarks</th> *@
                <th class="fw-bold">@Localizer["OperationsMsg"]</th>
            </tr>
        </thead>
        @* <tbody>
            @foreach (var obj in Model.OrderBy(x => x.Id))
            {
                <tr>
                    <td>@obj.ProductName</td>
                    <td>@obj.Season</td>
                    <td>@obj.Category.CatName</td> 
                    <td>@obj.SleeveType</td>                    
                    <td>@obj.Size</td>
                    <td>@obj.Price</td>
                    
                    <td>
                        <div class="w-auto btn-group-sm" role="group">
                            <a asp-controller="Product" asp-action="Upsert" asp-route-id="@obj.Id" class="btn btn-primary mx-2"> 
                                <i class="bi bi-pencil-square"></i>Edit
                            </a>

                            <a asp-controller="Product" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger">
                                <i class="bi bi-trash-fill"></i>Delete
                            </a>
                        </div>
                    </td>
                </tr>
            } 
        </tbody> *@
    </table>
</div>

@section Scripts{
    <script src="~/js/product.js"></script>
}
